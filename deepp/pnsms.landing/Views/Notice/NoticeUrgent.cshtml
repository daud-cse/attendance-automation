@model IEnumerable<pnsms.Entities.Models.Notice>
@foreach (var item in Model)
{
     <li>
     <p>@Html.ActionLink(@item.NoticeTitle, "Details", "Notice", new { id = item.Id }, null)</p> <time datetime="2008-02-14 20:00">@item.StartDate</time>
    </li>
    
}